# Tasks

Implementation detail — exact code, exact test bodies, and verified API signatures — lives in `docs/superpowers/plans/2026-07-25-nono-cedar-pdp-v1.md`. Task groups below map 1:1 to that plan's tasks. Every group is TDD: the failing test comes first.

Global gate for every group: `just lint && just test` must pass before the group's commit.

## 1. Scaffold and configuration

- [x] 1.1 Write failing `Config` tests in `src/config.rs`: minimal config applies defaults, backend→agent map with fallback, unknown key rejected, `~/` expanded. Verify: `cargo test --lib config` → fails with "cannot find type `Config`"
- [x] 1.2 Create `Cargo.toml` with the pinned dependency set, `[lib]` + `[[bin]]`, and the `clippy::unwrap_used`/`expect_used`/`panic` denials. Confirm `nono` appears only under `[dev-dependencies]`
- [x] 1.3 Implement `Config`, `ConfigError`, `expand_tilde`, `Config::agent_for`. Verify: `cargo test --lib config` → 4 passed
- [x] 1.4 Add `Justfile` (default recipe `just --list`, plus `check`/`test`/`lint`/`fmt`/`serve`) and `.gitignore`. Verify: `just check && just test && just lint` → all pass
- [x] 1.5 Commit: `feat: project scaffold and config loading`

## 2. Wire types and upstream conformance guard

- [x] 2.1 Write failing `src/wire.rs` tests using the real captured payloads: command envelope parses, endpoint envelope parses with proxy identity, unknown `capability_type` maps to `Unsupported`, unknown fields tolerated, response serializes to `{"decision":"allow"}` / `{"decision":"deny","reason":…}`. Verify: `cargo test --lib wire` → fails
- [x] 2.2 Implement `WebhookEnvelope`, `ApprovalRequest` (internally tagged on `capability_type`, `#[serde(other)]` → `Unsupported`), `CommandRequest`, `EndpointRequest`, `WebhookResponse`. No `deny_unknown_fields` anywhere in this module. Verify: `cargo test --lib wire` → 5 passed
- [x] 2.3 Write `tests/conformance.rs`: serialize `nono::ApprovalRequest::{Command,Endpoint,Capability}` with upstream's own serde, assert exact key sets, assert our mirrors round-trip, assert `capability` classifies as `Unsupported`, and assert `{"decision":"allow"}` does not parse as `nono::ApprovalDecision`. Verify: `cargo test --test conformance` → 4 passed (first run compiles the `nono` dev-dep, ~30 s)
- [x] 2.4 Commit: `feat: nono wire types with upstream conformance guard`

## 3. PolicyQuery boundary and webhook adapter

- [x] 3.1 Write failing `src/adapter/nono_webhook.rs` tests: command mapping, `caller_kind` derivation for a chained launch, unmapped backend falls back to unknown agent, endpoint mapping with proxy identity, unsupported variant errors with a deny reason, malformed body errors with a deny reason. Verify: `cargo test --lib adapter` → fails
- [x] 3.2 Implement `src/query.rs`: `PolicyQuery`, `Target`, `CallerKind`, `action_name()`, `resource_summary()`
- [x] 3.3 Implement `parse(body, config) -> Result<PolicyQuery, AdaptError>` and `AdaptError::deny_reason()`. Verify: `cargo test --lib` → all pass, including 6 adapter tests
- [x] 3.4 Commit: `feat: PolicyQuery boundary and nono webhook adapter`

## 4. Cedar schema

- [x] 4.1 Create `nono.cedarschema` as given in the plan: `Caller in Session in Agent`, `Command` with `args: Set<String>`, `HttpEndpoint`, actions `launchCommand`/`httpRequest` with optional `reason?` context. **Deviation from the plan text, per D12 amendment (task 11):** the plan's `argv: String` attribute is *not* in the shipped schema; the joined-string attribute is `argv_tail` (`args[1..]`)
- [x] 4.2 Write failing `src/cedar/schema.rs` tests: schema compiles and exposes both actions, a well-formed policy strict-validates, a set-membership argument policy validates, a policy referencing `resource.cwd` fails validation. Verify: `cargo test --lib cedar::schema` → fails
- [x] 4.3 Implement `SCHEMA_SRC` (`include_str!`), `load()` surfacing `SchemaWarning`s via `tracing::warn`, `SchemaLoadError`. Verify: `cargo test --lib cedar::schema` → 4 passed
- [x] 4.4 Commit: `feat: embedded Cedar schema for nono approval requests`

## 5. Policy loading, validation, and the validate CLI

- [x] 5.1 Write failing `src/cedar/engine.rs` loader tests: provenance ids from `@id`/ordinal, non-`.cedar` files ignored, empty dir is `PolicyLoadError::Empty`, syntax error names the file, schema violation is `Validation`, duplicate ids in one file are `Duplicate`, `bootstrap` exposes a snapshot at generation 1. Verify: `cargo test --lib cedar::engine` → fails
- [x] 5.2 Implement `LoadedPolicies`, `PolicyLoadError`, `load_dir(dir, schema, generation)` using `PolicySet::from_str` per file plus `Policy::new_id` (no manual `;` splitting), then `Validator::new(schema.clone()).validate(…, ValidationMode::Strict)`
- [x] 5.3 Implement `Engine::{bootstrap, snapshot, schema, policy_dir, reload}` over `ArcSwap<LoadedPolicies>`. Verify: `cargo test --lib cedar::engine` → 7 passed
- [x] 5.4 Add the clap CLI skeleton in `src/main.rs` with the `validate` subcommand and `tracing_subscriber` init. Verify: `cargo run -- validate --config ./nono-cedar-pdp.toml` against a one-policy dir → `OK: 1 policies loaded and validated`, exit 0
- [x] 5.5 Commit: `feat: policy loading with strict validation and validate CLI`

## 6. Entity building, decisions, and the check CLI

- [x] 6.1 Write failing evaluation tests in `src/cedar/engine.rs`: allow names the permitting policy (`matrix:allow-git`), forbid names the forbidding policy, nothing-matched gives an empty matched list and a "no policy" reason, unmapped agent denied, endpoint GET allowed and DELETE denied, `eval_us > 0`. Verify: `cargo test --lib cedar::engine` → fails
- [x] 6.2 Implement `src/decision.rs`: `Decision{allow, matched, reason, eval_us}`, `deny()`, `from_response()` (sorted matched ids; **evaluation errors force deny even on Allow**), `to_wire()`
- [x] 6.3 Implement `src/cedar/entities.rs`: `build(query, schema) -> (Request, Entities)` with the `Caller in Session in Agent` slice, resource attributes, context pairs (omitting `reason` when absent), and identifier escaping of `\` and `"`
- [x] 6.4 Implement `Engine::evaluate` — never returns an error; entity-build failure becomes a logged deny. Verify: `cargo test --lib` → all pass
- [x] 6.5 Add the `check <fixture>` subcommand; create `tests/fixtures/git-status.json`. Verify: `cargo run -- check --config ./nono-cedar-pdp.toml tests/fixtures/git-status.json` → `ALLOW: permitted by …`
- [x] 6.6 Commit: `feat: cedar entity building, decisions, and check CLI`

## 7. Decision audit log

- [x] 7.1 Write failing `src/audit.rs` tests: two decisions produce two parseable lines with the full field set and an RFC 3339 timestamp; the created file is mode `0600`; a missing parent directory is created. Verify: `cargo test --lib audit` → fails
- [x] 7.2 Implement `AuditLog::open` (creates parents, `0600`, append) and `AuditLog::record` (serialize failure and write failure are logged and swallowed, never altering a decision), plus `AuditRecord`. Verify: `cargo test --lib audit` → 2 passed
- [x] 7.3 Wire the audit log into `check`. Verify: run `check`, then `tail -1 ./decisions.jsonl` → a line containing `"decision":"allow"`
- [x] 7.4 Commit: `feat: JSONL decision audit log`

## 8. HTTP decision endpoint

- [x] 8.1 Write failing `tests/server.rs`: permitted command → `200 {"decision":"allow"}`; unpermitted → `200` deny with a "no policy" reason; malformed body → **`200` deny, not 4xx**; unsupported variant → `200` deny; every decision audited (exactly one line); `/healthz` reports generation 1 and the number of policies in the test set (1 then, 2 since task 12.6 added an endpoint permit to it). Verify: `cargo test --test server` → fails
- [x] 8.2 Implement `src/server.rs`: `AppState`, `router()` with `CatchPanicLayer`, `POST /v1/approve` taking `Bytes` (**not** `Json<T>`, so a malformed body yields our deny reason instead of axum's 400), `GET /healthz`, and the defensive empty-policy-set `503` guard
- [x] 8.3 Implement `serve()` binding a `TcpListener` and add the `serve` subcommand with a tokio runtime. Verify: `cargo test --test server` → 6 passed
- [x] 8.4 Manual smoke: start `serve`, then `curl /healthz`, `curl -d @tests/fixtures/git-status.json /v1/approve`, `curl -d 'garbage' /v1/approve`. Verify: health JSON, `{"decision":"allow"}`, `{"decision":"deny","reason":"malformed…"}`
- [x] 8.5 Commit: `feat: fail-closed HTTP decision endpoint with health check`

## 9. Policy hot-reload

- [x] 9.1 Write failing reload tests in `src/cedar/engine.rs`: a valid edit advances the generation and changes the decision; a syntax-error edit keeps the last-good set and does not advance the generation; a schema-violating edit likewise. Verify: `cargo test --lib cedar::engine::tests::reload` → these three are the D7 contract and must pass
- [x] 9.2 Write a failing `src/watcher.rs` test: an edit to a policy file causes a reload within 5 s. Verify: `cargo test --lib watcher` → fails
- [x] 9.3 Implement `watcher::spawn(engine)` — `notify` watcher on the policy dir, 150 ms debounce draining the editor's event burst, reload errors logged without replacing the active set. Verify: `cargo test --lib watcher` → passes
- [x] 9.4 Wire the watcher into `serve`, bound to `_watcher` so it is not dropped immediately. Verify manually: `/healthz` shows generation 1, append a valid policy → generation 2, append broken Cedar → still generation 2
- [x] 9.5 Commit: `feat: policy hot-reload keeping last-good set on failure`

## 10. Starter policies, nono wiring, and end-to-end proof

- [x] 10.1 Write `policies/00-baseline.cedar` (session-launches-only forbid; deny unmapped `Agent::"unknown"`) and `policies/10-git.cedar` (read-only git permit; forbid `--force`/`--force-with-lease`/`--hard` by set membership). Remove the scratch `policies/starter.cedar` with `trash`. Verify: `cargo run -- validate --config ./nono-cedar-pdp.toml` → `OK: 4 policies loaded and validated` — **superseded by task 12**: the read-only permit's set membership was a wrong-allow (`git -c core.fsmonitor=<cmd> status`), so the shipped permit now pins the subcommand on `argv_tail` and a fifth policy forbids git's code-executing flags; `validate` reports `OK: 5 policies loaded and validated`
- [x] 10.2 Add `tests/fixtures/git-force-push.json`. Verify: `check` on `git-status.json` → `ALLOW: permitted by 10-git:git-read-only`; `check` on `git-force-push.json` → `DENY: denied by 10-git:no-history-rewrites`
- [x] 10.3 Generate the nono profile with `nono profile init cedar-pdp-smoke`, merge in the `command_policies` block (webhook + chain + terminal backends, `approval_defaults.backend`, `git` intercept rules on `status` and `push`), copy to `examples/cedar-pdp-smoke.json`. Verify: `nono profile validate examples/cedar-pdp-smoke.json` → OK. nono's own schema is authoritative over the plan if they disagree — **corrected by task 14.5**: the profile shipped only the `any`-mode chain (`cedar-or-ask`), so the `cedar-and-ask` posture the README documents did not exist; it now defines both chains
- [x] 10.4 Add the `just smoke` recipe: start the daemon, wait for `/healthz`, run `nono run --profile cedar-pdp-smoke -- git status` (expect success), run `nono run … -- git push --force` (expect block), assert both an allow and a deny line in the audit log. Verify: `just smoke` → `SMOKE PASSED`
- [x] 10.5 Write `README.md` covering: what it is (nono = PEP, Cedar = PDP); the verified contract with a real payload (`args[0]` shown as the per-run shim path it really is); quick start; the nono profile snippet; the three rollout postures; **the schema caveats** (`args` is a set so no positional matching; there is no whole-argv attribute, so anchored globs go on `argv_tail` and a policy reading `resource.argv` fails validation; `argv_tail` globs are forbid-only — the loader warns about any `permit` that reads `argv_tail`, and about an `args` membership literal containing `/`; endpoint requests have no session identity, so the daemon *pins* `Caller/Session::"proxy"` instead of trusting the payload); the identity limitation that nono sends a caller *label* not a kind, so a profile intercepting a command literally named `session` is indistinguishable from a direct launch; the unauthenticated-webhook risk and the https-on-loopback follow-up; pointers to spec, ADR and research docs
- [x] 10.6 Final gate: `just fmt && just lint && just test && just smoke` → all green. Commit: `feat: starter policy pack, nono profile, README, e2e smoke test`

## 11. Post-audit: the args[0] contract, `argv_tail`, and removing `argv` (D12)

Post-implementation audit finding, verified against real runtime data: nono sends `args[0]` as an absolute per-run shim path (`/private/tmp/nono-tool-sandbox-<pid>-<nanos>-<hex>/shims/git`), not the command name. The `["git", "push"]` shape this project modelled came from upstream's *unit-test* fixture. Consequence: `resource.command` is unaffected and unanchored globs still work, but every start-anchored pattern (a whole-argv glob, `args.contains("git")`) never matches at runtime — fail-safe in a `permit`, **fail-open in a `forbid`**, which is exactly what the README recommended for forbid rules.

- [x] 11.1 Add `argv_tail: String` to the `Command` entity in `nono.cedarschema` and **delete `argv`**; keep `args` (the faithful Set, `args[0]` included) and `arg_count`; move the forbid-only comment onto `argv_tail`, which inherits the flattening hazard. Verify: `cargo test --lib cedar::schema` → the new `a_policy_reading_argv_is_refused_by_strict_validation` and `an_anchored_argv_tail_policy_strict_validates` pass
- [x] 11.2 Populate `argv_tail` in `src/cedar/entities.rs` with `args[1..]` joined by a single space (`""` when `args` has fewer than two entries); stop populating `argv`. Verify: `cargo test --lib cedar::entities` → 6 passed, incl. `argv_tail_excludes_the_per_run_shim_path` and `argv_tail_is_empty_when_there_is_no_tail`
- [x] 11.3 Retarget the loader lint (`engine::lint_argv_in_permit` → `engine::lint_arg_matching`): the anchored-`argv` lint is unnecessary because strict validation rejects `argv` outright (proved by a test instead); keep the `permit`-reads-`argv_tail` lint for the surviving flattening hazard; add a lint for an `args` membership test whose literal contains `/`, since `args` still holds the per-run shim path. Verify: `cargo test --lib cedar::engine` → 26 passed
- [x] 11.4 Move every fixture and test onto the runtime shape — `tests/fixtures/*.json`, `tests/{conformance,server,policies}.rs`, and the `args` literals in `src/{wire,adapter/nono_webhook,audit,query}.rs` and `src/cedar/*` — via `wire::EXAMPLE_SHIM_ARGV0`. The conformance key-set assertions are unchanged; only the values move. Verify: `cargo test` → 102 passed (95 before, +7 new); `cargo run -- check` on both fixtures still `ALLOW: permitted by 10-git:git-read-only` / `DENY: denied by 10-git:no-history-rewrites`
- [x] 11.5 Update the docs to say precisely what this fixes: design spec §2 correction + D12 (removal, with the four reasons) + §5 schema and caveats; `cedar-policy-evaluation` requirements (validation rejects `argv`; the two surviving lints) and `pdp-operations` documentation requirements; the change's `design.md` D6/risks; `openspec/config.yaml` contract facts; README (real payload + four caveats); `policies/10-git.cedar` comments; the plan's ground-truth block. Verify: `openspec validate --changes add-cedar-pdp-v1` → passes; `just lint` clean

## 12. Post-audit: two wrong-allow holes — subcommand position and raw endpoint paths (D14, D15)

Adversarial security audit findings, both reproduced against the running daemon. (1) `10-git:git-read-only` tested set membership, which cannot express POSITION, so `git -c core.fsmonitor=<cmd> status` was allowed — and git runs `<cmd>`, i.e. arbitrary code execution behind a "read-only git" permit. `argv_tail` (task 11) is the primitive that can express position: it strips `args[0]`, so its first token IS the subcommand. (2) endpoint `path` reaches policy raw and still percent-encoded, so `resource.path like "/repos/*"` — the shape the design doc, the README and the tests all use — was satisfied by `/repos/../user/keys`, which a normalising origin serves as `/user/keys`.

- [x] 12.1 Write the failing policy-pack tests in `tests/policies.rs`: the `-c core.fsmonitor=… status` argv is denied and the deny names `10-git:no-code-executing-git-flags`; the read-only subcommands (`status`, `status --porcelain`, `diff --stat`, `log -n 5`, `show HEAD`) are still allowed by `10-git:git-read-only`; every argv the audit found allowed by a stray read-only word (`commit -m status`, `commit --amend -m log`, `reset --soft status`, `clone 'ext::sh -c evil' status`) is denied; **each layer denies the exploit with the other removed**; the shipped pack reports no load lints; the pack now holds 5 policies. Verify: `cargo test --test policies` → 5 failed, the exploit failing as `allow: true, matched: ["10-git:git-read-only"]`
- [x] 12.2 Rewrite the `10-git:git-read-only` permit to pin the subcommand positionally — `resource.argv_tail == "<sub>" || resource.argv_tail like "<sub> *"` for `status`/`diff`/`log`/`show` — and add `@id("no-code-executing-git-flags")`: `forbid` on `resource.command == "git"` with exact membership for `-c` (git requires a separate argv entry) and `argv_tail` globs for `--config-env`/`--exec-path`/`--upload-pack`/`--receive-pack`, whose `--flag=<value>` spelling membership cannot see. Document the deliberate over-denial (`git log -c`) and why `--git-dir`/`-C`/`ext::` are not listed. Verify: `cargo test --test policies` → 10 passed, 1 failed (the shipped pack now trips the old lint)
- [x] 12.3 Narrow the loader lint that fired on the fix: the flattening hazard is *searching* a joined string, so flag only a `permit` whose `argv_tail` test does not pin a whole token. New `unpinned_argv_tail_reads` walks the policy's JSON EST, so a comment is still not a read and a mixed permit is still flagged. A pin is `==`, a wildcard-free `like`, or a pattern whose literal ends at the separating space; `like "diff*"` is **not** a pin — it also approves `git difftool --extcmd=<cmd>`, so it is flagged too. Verify: `cargo test --lib cedar::engine` → 29 passed, incl. `a_positionally_anchored_argv_tail_permit_is_not_linted` and `an_anchored_permit_that_stops_mid_token_is_linted`; `cargo test --test policies` → 11 passed
- [x] 12.4 Write the failing endpoint-path tests, then implement `src/endpoint_path.rs`: `ambiguity(path)` reports a `.`/`..` segment (`;`-parameters stripped) at any percent-decode depth up to 8, a malformed escape in the path as sent, a decode yielding non-UTF-8 bytes, and nesting past the bound — while leaving the query string, dots inside a segment and a post-first-pass stray `%` alone. Verify: stubbing `ambiguity` to `None` fails 5 of the 7 unit tests; the implementation passes all 7
- [x] 12.5 Deny in `Engine::evaluate` **before** entities are built or any policy is consulted, with a reason naming the ambiguity and the path as sent; keep `path` raw for the policies that match it. Verify: `cargo test --lib cedar::engine::tests::an_ambiguous` → failed first with `allow: true, matched: ["matrix:allow-github-reads"]` for `/repos/../user/keys`, passes after
- [x] 12.6 Cover the wire and the CLI: `tests/server.rs` gains an endpoint permit plus `200 {"decision":"deny"}` with the ambiguity reason and an audit line carrying the raw path; `tests/cli.rs` gains a `check` run on the hand-written `tests/fixtures/endpoint-traversal.json` (deny, non-zero exit, reason names the ambiguity) and one pinning the two command fixtures' documented decisions. Verify: `cargo test` → 122 passed (102 before); `cargo run -- check --config ./nono-cedar-pdp.toml tests/fixtures/endpoint-traversal.json` → `DENY: ambiguous endpoint path "/repos/../user/keys": …`, exit 1
- [x] 12.7 Update the docs to the shipped behaviour: `nono.cedarschema` (anchored tests pin the subcommand; unanchored globs are forbid-only; `path` is raw and an ambiguous one is denied); README caveats (now six, with the position and raw-path caveats, and `policies` count 5); `cedar-policy-evaluation` (lint requirement narrowed, ambiguity requirement rewritten to any-depth + non-UTF-8 + the deliberate non-ambiguities) and `pdp-operations` (new starter-pack requirement, documentation scenarios); the change's `design.md` D6/risks; design spec D14/D15 + §5 caveats + fail-closed matrix; `openspec/config.yaml`; the plan's supersession note. Verify: `openspec validate --changes add-cedar-pdp-v1 --strict` → passes; `just lint` clean; `just test` green

## 13. Post-audit: the daemon's own state — policy source isolation and an audit trail that survives rotation (D13, D16)

Adversarial security audit findings, both reproduced against the running daemon. (1) **Critical.** The shipped `policy_dir = "./policies"` / `audit_log = "./decisions.jsonl"` sat inside the tree `examples/cedar-pdp-smoke.json` grants the sandboxed `git` (`workdir.access: "readwrite"` plus `fs_write: ["."]`), so a process *inside the sandbox* overwrote both starter policies, the watcher reloaded them, and a `git push --force` that had been denied came back `allow`. (2) **Major.** The audit fd was opened once and never revalidated, so renaming the log left every later decision appended to a detached inode — answered, unrecorded at the configured path, `/healthz` still green.

Path note for the earlier records: the `--config ./nono-cedar-pdp.toml` invocations in tasks 5.4, 6.5, 7.3, 10.1 and 12.6 now resolve `~/.config/nono-cedar-pdp/policies` and `~/.local/state/nono-cedar-pdp/decisions.jsonl`. Reproduce them with `just install-policies` first, or against `--config ./nono-cedar-pdp.dev.toml`, which keeps the repo-relative paths those lines were written for.

- [x] 13.1 Write the failing audit-rotation tests in `src/audit.rs`: a renamed log is reopened at the configured path (0600, the archived file receives nothing further, the operator sees it), a deleted log is recreated, a reopen that cannot succeed still records and says so, an in-place truncation is reported as a shrink. Verify: `cargo test --lib audit` → 4 failed with "nothing readable at the configured path: No such file or directory" and empty operator logs
- [x] 13.2 Implement the reattach in `src/audit.rs`: `LogFile` carries `(st_dev, st_ino)` beside the handle, `ByteSink::reattach` re-stats the path **before every record** (one `stat` against a millisecond-scale Cedar evaluation; "periodic" would leave a window of unrecorded decisions) and reopens on a mismatch or a missing file, resetting the mid-record and size state for the new file. A failed reopen keeps appending to the handle already held and logs an error — dropping the line would lose more. Verify: `cargo test --lib audit` → 10 passed
- [x] 13.3 Cover it over HTTP in `tests/server.rs` (`decisions_after_a_log_rotation_still_land_at_the_configured_path`): one router, two requests, `rename` in between; the second decision is unchanged and is the only line at the configured path. Verify: with `reattach` stubbed out the test fails with `left: 0, right: 1` (nothing at the configured path); with it, `cargo test --test server` → 17 passed
- [x] 13.4 Write the failing `src/isolation.rs` tests, then implement `check(policy_dir, audit_log, cwd)`: refuse on a group- or world-writable policy directory or loadable policy file (naming path, mode and `chmod go-w`), refuse when the directory cannot be stat'd, ignore `.cedar` names the loader skips (shares `engine::is_loadable_policy_file`, now `pub(crate)`), follow symlinks to the target's mode, and warn — loudly, with the limits stated — for a `policy_dir` or `audit_log` that resolves inside the cwd. Paths are resolved through the longest existing prefix so `/var` vs `/private/var` cannot make the comparison lie. Verify: 9 of 11 failed against the stub, 11 passed after
- [x] 13.5 Call it from `run_serve` **before** the schema, the policy load, the audit log and the listener, refusing on `Err` and logging each warning at WARN. Verify: `cargo test --test cli` → 6 passed, incl. `a_group_writable_policy_directory_refuses_to_start` (exit non-zero, message names path/mode/remedy) and `a_policy_directory_inside_the_working_directory_warns_before_serving` (the SECURITY warning precedes the audit-log failure, proving the ordering). `serve_from` now runs the daemon under a 30 s deadline and kills it, so a regression that lets one of these start fails the suite instead of hanging it
- [x] 13.6 Move the shipped config to `~/.config/nono-cedar-pdp/policies` + `~/.local/state/nono-cedar-pdp/decisions.jsonl`, add `nono-cedar-pdp.dev.toml` (repo-relative, header says why it is dev-only) with `just serve-dev`, add `just install-policies` (`cp -n`, 0700/0600), and rewrite `just smoke` to build its own state under `~/.cache/nono-cedar-pdp/smoke`, assert that no write grant in the resolved profile contains it, and read its assertions from the configured audit-log path. Verify: `just install-policies` → 2 policies at 0600; `cargo run -- validate` → `OK: 5 policies loaded and validated`
- [x] 13.7 Document both checks **and their limits** in the same words everywhere — `src/isolation.rs` module docs, design spec D13 + new D16, `pdp-operations` and `decision-audit-log` spec requirements, README "Keep the policy directory out of the sandbox": the mode refusal defends against *other local users* and does nothing about the agent (path-based sandboxes do not change uid, so it runs as the same user); the cwd warning is a heuristic proxy that misses an absolute path inside a granted tree (macOS default groups grant write to `/tmp`, `$TMPDIR`, `/var/folders`) and fires when no agent exists; the only real control is the profile, with the runnable procedure for checking one (`nono profile show --format manifest` filtered to write grants, **plus** every `command_policies.commands.*.from.*.sandbox.fs_write`/`fs_write_file`, which the resolved manifest does not include — verified against nono 0.69.0). Verify: `openspec validate --changes add-cedar-pdp-v1 --strict` → passes; both jq snippets run against `examples/cedar-pdp-smoke.json`
- [x] 13.8 Final gate: `just lint` clean, `just test` green, plus hand-driven `serve` runs showing the warning (dev config, repo cwd) and the refusal (`chmod g+w ~/.config/nono-cedar-pdp/policies` → `FAIL: … is group-writable (mode 0720) … chmod go-w …`, exit 1)

## 14. Post-audit: the scenarios nothing tested (spec-coverage findings)

A spec-coverage audit of the shipped v1 found 15 scenarios that were unguarded, covered
by construction, or asserted as a type rather than as the behaviour the scenario states.
Two were fail-closed guarantees with no test at all — the `serve` startup ordering and
the `validate` gate — and one was a documentation posture that had already rotted.

Every test below was verified by mutating the production code and watching it fail for
the stated reason; the mutations are named in each item.

- [x] 14.1 Cover the two fail-closed startup guarantees in `tests/cli.rs`: `validate` on an invalid policy directory exits non-zero and prints the validation errors (schema violation names the attribute and the policy id, an unparseable file names the file, an empty directory says so), and a policy load failure exits without binding a port — proven by the absence of the `listening` line, by the address still being free, and by a variant that *holds* the port so a `bind` moved ahead of the load would report `Address already in use` instead. Verify: mutations — an early `TcpListener::bind` in `run_serve` (both port tests fail, one on the `listening` line and one on `Address already in use`) and a `validate` whose `Err` arm exits `SUCCESS` without the message
- [x] 14.2 Cover `validate`'s success path in the same file: exit 0 and the printed count equal to the number `engine::load_dir` actually loads (computed in-test, so the number is not hard-coded). Verify: mutation — `count + 1` in the printed line fails it
- [x] 14.3 Bind a real socket in two places, since every HTTP test drove the `Router` through `oneshot` and `server::serve` had zero test callers: `tests/server.rs` calls `serve` on an ephemeral loopback port and drives `GET /healthz` + `POST /v1/approve` over a hand-written HTTP/1.1 exchange, and `tests/cli.rs` runs the whole binary and asserts it answers on the address its configuration named, with both fixture decisions landing in the configured audit log. Pinned to an ephemeral port, not `127.0.0.1:8181`: the documented default is where an operator runs their own daemon, and `config::tests::loads_minimal_config_with_defaults` already pins the default value. Verify: mutation — a `serve` that returns without binding fails both
- [x] 14.4 Cover the audit line's `matched` and `eval_us` and the whole `httpRequest` path in `tests/server.rs`: a decided command line names the permitting policy id and reports a non-zero evaluation time, a default deny carries an explicit empty list, and an `endpoint` envelope posted to `/v1/approve` returns 200 allow and is audited with `action` `httpRequest`, the pinned proxy identity, the matched permit and the resource summary. Verify: mutations — `matched: &[]` and `eval_us: 0` in `AuditRecord`
- [x] 14.5 Add the documentation guard that would have caught the rot, `tests/docs.rs`, then fix the rot: the README's rollout table named `cedar-and-ask` and `examples/cedar-pdp-smoke.json` never defined it, so the documented Paranoid posture was a step no operator could take. The guard parses the table and asserts every backend it names is defined in the shipped profile (and the reverse), that both chain modes (`any` and `all`) exist with resolvable members, that the README's `command_policies` snippet defines the same backends as the profile, and that the caveat and risk passages the spec's documentation scenarios require are still present. Verify: the two guard tests failed on the missing backend; `nono profile validate` on the profile with `approval_defaults.backend = "cedar-and-ask"` went from `unknown_approval_backend`, exit 1 to `Result: valid`, exit 0
- [x] 14.6 Replace the by-construction and by-type coverage: an unwritable audit log leaves the decision and the HTTP response unchanged (driven through the handler with a zero-byte sink, not inferred from `record` returning `()`); the *watcher's* failure branch keeps the last-good set and keeps watching (a repair afterwards proves the thread survived); the evaluation-error override pins that Cedar's own decision was `Allow` and that the overridden permit is still named; a payload that cannot become a Cedar request denies with a reason that says so and logs the error; `caller_kind` is asserted as the context string `"command"`/`"session"` a policy reads, and through a policy that decides on it; and the duplicate-id and unknown-config-key errors are asserted on their message, not their variant. Verify: mutations — a panicking audit write (500 instead of 200), a watcher thread that returns after a failed reload, `matched` cleared on the error path, the build-error log removed, swapped `CallerKind::as_str` arms, and both error `Display`s stripped of the file and key they name
- [x] 14.7 Gate: `cargo test` → 157 passed (140 before: +5 `tests/cli.rs`, +4 `tests/server.rs`, +4 lib, +4 the new `tests/docs.rs`), 3 existing tests strengthened; `just lint` clean; `openspec validate --changes add-cedar-pdp-v1 --strict` passes
